name: Deploy to Production

on:
  push:
    branches:
      - main
  workflow_dispatch:  # 允许手动触发

jobs:
  deploy:
    name: Deploy to Server
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Deploy to server
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SERVER_SSH_KEY }}
          port: ${{ secrets.SSH_PORT || 22 }}
          script_stop: true
          script: |
            set -e
            set -x  # 启用调试模式，显示执行的命令
            
            echo "=========================================="
            echo "开始部署 hbgm001"
            echo "=========================================="
            
            # 项目目录（尝试多个可能的位置）
            if [ -d "/home/ubuntu/hbgm001" ]; then
              PROJECT_DIR="/home/ubuntu/hbgm001"
              SERVICE_NAME="hbgm001-backend"
            elif [ -d "/opt/luckyred" ]; then
              PROJECT_DIR="/opt/luckyred"
              SERVICE_NAME="luckyred-api"
            else
              echo "❌ 错误: 找不到项目目录"
              echo "尝试查找项目目录..."
              find /home /opt -type d -name "hbgm001" -o -name "luckyred" 2>/dev/null | head -5
              exit 1
            fi
            
            echo "📁 使用项目目录: $PROJECT_DIR"
            echo "🔧 使用服务名称: $SERVICE_NAME"
            
            # 进入项目目录
            cd "$PROJECT_DIR" || {
              echo "❌ 错误: 无法进入项目目录: $PROJECT_DIR"
              exit 1
            }
            
            # 拉取最新代码
            echo "📥 拉取最新代码..."
            git fetch origin main || {
              echo "❌ Git fetch 失败"
              exit 1
            }
            git reset --hard origin/main || {
              echo "⚠️  Git reset 失败，尝试 git pull..."
              git pull origin main || {
                echo "❌ Git pull 失败"
                exit 1
              }
            }
            echo "✅ 代码更新成功"
            
            # 激活虚拟环境并更新依赖
            echo "📦 更新 Python 依赖..."
            
            # 检查虚拟环境位置（可能在项目根目录或 api 子目录）
            if [ -d "$PROJECT_DIR/.venv" ]; then
              VENV_DIR="$PROJECT_DIR/.venv"
            elif [ -d "$PROJECT_DIR/api/.venv" ]; then
              VENV_DIR="$PROJECT_DIR/api/.venv"
            else
              echo "⚠️  虚拟环境不存在，创建新的虚拟环境..."
              VENV_DIR="$PROJECT_DIR/.venv"
              python3 -m venv "$VENV_DIR"
            fi
            
            source "$VENV_DIR/bin/activate"
            echo "✅ 虚拟环境已激活: $VENV_DIR"
            
            # 安装依赖
            echo "📦 升级 pip..."
            pip install --upgrade pip || {
              echo "⚠️  pip 升级失败，继续..."
            }
            echo "📦 安装 Python 依赖..."
            
            # 检查 requirements.txt 位置
            if [ -f "$PROJECT_DIR/requirements.txt" ]; then
              REQUIREMENTS_FILE="$PROJECT_DIR/requirements.txt"
            elif [ -f "$PROJECT_DIR/api/requirements.txt" ]; then
              REQUIREMENTS_FILE="$PROJECT_DIR/api/requirements.txt"
            else
              echo "⚠️  未找到 requirements.txt，跳过依赖安装"
              REQUIREMENTS_FILE=""
            fi
            
            if [ -n "$REQUIREMENTS_FILE" ]; then
              pip install -r "$REQUIREMENTS_FILE" || {
                echo "⚠️  依赖安装失败，尝试使用 --no-cache-dir..."
                pip install -r "$REQUIREMENTS_FILE" --no-cache-dir || {
                  echo "❌ 依赖安装失败"
                  exit 1
                }
              }
              echo "✅ Python 依赖安装完成"
            fi
            
            # 构建前端
            echo "🏗️  构建前端..."
            if [ -d "$PROJECT_DIR/frontend" ]; then
              cd "$PROJECT_DIR/frontend"
              # 检查 node_modules 是否存在，如果不存在则安装
              if [ ! -d "node_modules" ]; then
                echo "📦 安装前端依赖..."
                npm install || {
                  echo "⚠️  npm install 失败，尝试使用 --legacy-peer-deps..."
                  npm install --legacy-peer-deps || true
                }
              else
                echo "📦 更新前端依赖..."
                npm install || npm install --legacy-peer-deps || true
              fi
              # 构建前端
              echo "🔨 执行前端构建..."
              npm run build || {
                echo "⚠️  前端构建失败，查看错误..."
                exit 1
              }
              echo "✅ 前端构建完成"
              cd "$PROJECT_DIR"
            else
              echo "⚠️  前端目录不存在，跳过前端构建"
            fi
            
            # 重启服务
            echo "🔄 重启服务..."
            
            # 检查服务是否存在，如果不存在则尝试其他服务名
            if ! systemctl list-unit-files | grep -q "^${SERVICE_NAME}.service"; then
              echo "⚠️  服务 $SERVICE_NAME 不存在，尝试查找其他服务..."
              # 尝试其他可能的服务名
              if systemctl list-unit-files | grep -q "luckyred-api.service"; then
                SERVICE_NAME="luckyred-api"
                echo "✅ 找到服务: $SERVICE_NAME"
              elif systemctl list-unit-files | grep -q "hbgm001-backend.service"; then
                SERVICE_NAME="hbgm001-backend"
                echo "✅ 找到服务: $SERVICE_NAME"
              else
                echo "❌ 错误: 找不到服务，可用的服务列表:"
                systemctl list-unit-files | grep -E "luckyred|hbgm001" || true
                echo "⚠️  跳过服务重启，请手动重启服务"
              fi
            fi
            
            if [ -n "$SERVICE_NAME" ]; then
              echo "🔄 重启服务: $SERVICE_NAME"
              sudo systemctl restart "$SERVICE_NAME" || {
                echo "❌ 服务重启失败，查看日志..."
                sudo journalctl -u "$SERVICE_NAME" --no-pager -n 50 || true
                echo "⚠️  服务重启失败，但部署继续..."
              }
              echo "✅ 服务重启命令执行成功"
              
              # 等待服务启动
              sleep 3
              
              # 检查服务状态
              echo "✅ 检查服务状态..."
              if sudo systemctl is-active --quiet "$SERVICE_NAME"; then
                echo "✅ 服务运行正常"
                sudo systemctl status "$SERVICE_NAME" --no-pager -l || true
              else
                echo "⚠️  服务未运行，查看日志..."
                sudo journalctl -u "$SERVICE_NAME" --no-pager -n 50 || true
                echo "⚠️  服务未运行，但部署继续..."
              fi
            fi
            
            echo "=========================================="
            echo "部署完成！"
            echo "=========================================="
