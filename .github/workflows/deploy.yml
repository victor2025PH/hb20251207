name: Deploy to Production

on:
  push:
    branches:
      - master  # 根据你的主分支名称调整（main 或 master）
  workflow_dispatch:  # 允许手动触发

jobs:
  deploy:
    name: Deploy to Server
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Deploy to server
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USERNAME }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: ${{ secrets.SSH_PORT || 22 }}
          script: |
            set -e
            
            echo "=========================================="
            echo "开始部署 LuckyRed API"
            echo "=========================================="
            
            # 项目目录
            PROJECT_DIR="/opt/luckyred"
            API_DIR="$PROJECT_DIR/api"
            
            # 进入项目目录
            cd $PROJECT_DIR
            
            # 拉取最新代码
            echo "📥 拉取最新代码..."
            git fetch origin
            git reset --hard origin/master  # 根据你的主分支名称调整
            
            # 激活虚拟环境并更新依赖
            echo "📦 更新 Python 依赖..."
            if [ -d "$API_DIR/.venv" ]; then
              source $API_DIR/.venv/bin/activate
              pip install --upgrade pip
              pip install -r $API_DIR/requirements.txt
            else
              echo "⚠️  虚拟环境不存在，创建新的虚拟环境..."
              python3 -m venv $API_DIR/.venv
              source $API_DIR/.venv/bin/activate
              pip install --upgrade pip
              pip install -r $API_DIR/requirements.txt
            fi
            
            # 运行数据库迁移（如果有）
            echo "🗄️  检查数据库迁移..."
            if [ -d "$PROJECT_DIR/migrations" ]; then
              echo "发现 migrations 目录，请手动运行迁移脚本"
            fi
            
            # 重启服务
            echo "🔄 重启服务..."
            sudo systemctl restart luckyred-api || echo "⚠️  服务重启失败，请检查服务状态"
            
            # 等待服务启动
            sleep 3
            
            # 检查服务状态
            echo "✅ 检查服务状态..."
            sudo systemctl status luckyred-api --no-pager -l || true
            
            echo "=========================================="
            echo "部署完成！"
            echo "=========================================="

