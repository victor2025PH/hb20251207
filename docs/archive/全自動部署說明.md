# 全自動部署說明

## 🚀 快速部署方式

### 方式一：一鍵部署（推薦）

在本地 Windows 上執行：

```powershell
# 方式 1: 雙擊運行
deploy.bat

# 方式 2: PowerShell
.\deploy-now.ps1

# 方式 3: 完整自動化
.\auto-deploy.ps1
```

### 方式二：服務器端直接執行

SSH 連接到服務器後執行：

```bash
ssh ubuntu@165.154.254.99
cd /opt/luckyred
bash deploy/scripts/auto-deploy.sh
```

## 📋 部署流程

1. **檢查 Git 狀態** - 確認本地代碼狀態
2. **推送代碼** - 將最新代碼推送到 GitHub
3. **拉取代碼** - 在服務器上拉取最新代碼
4. **構建前端** - 安裝依賴並構建前端
5. **重啟服務** - 重啟 API、Bot 和 Admin 服務
6. **檢查狀態** - 確認所有服務運行正常

## ✅ 部署後檢查

1. 訪問 MiniApp: https://mini.usdt2026.cc
2. 進入「發送紅包」頁面
3. 確認遊戲規則彈窗自動顯示
4. 測試「以後不再彈出」選項

## 🔧 如果遇到問題

### SSH 連接問題

如果 SSH 需要密碼，請：
1. 配置 SSH 密鑰認證
2. 或手動輸入密碼

### 服務啟動失敗

檢查服務日誌：
```bash
sudo journalctl -u luckyred-api -n 50
sudo journalctl -u luckyred-bot -n 50
```

### 前端構建失敗

檢查 Node.js 版本：
```bash
node --version  # 應該 >= 16
npm --version
```
