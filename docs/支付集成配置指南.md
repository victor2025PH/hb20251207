# 💳 支付集成配置指南

## Alchemy Pay 配置

### 1. 获取API凭证

1. 注册Alchemy Pay账户：https://alchemypay.org
2. 在控制台获取：
   - API Key
   - API Secret
   - Merchant ID

### 2. 配置环境变量

在`.env`文件中添加：

```env
# Alchemy Pay配置
ALCHEMY_PAY_API_KEY=your_api_key_here
ALCHEMY_PAY_API_SECRET=your_api_secret_here
ALCHEMY_PAY_MERCHANT_ID=your_merchant_id_here
ALCHEMY_PAY_BASE_URL=https://api.alchemypay.org

# API基础URL（用于Webhook回调）
API_BASE_URL=https://mini.usdt2026.cc
```

### 3. 配置Webhook

在Alchemy Pay控制台设置Webhook URL：
```
https://your-domain.com/api/v1/payment/webhook/alchemy
```

### 4. 测试

```bash
# 测试支付API
curl -X POST http://localhost:8080/api/v1/payment/fiat \
  -H "Content-Type: application/json" \
  -H "X-Telegram-Init-Data: ..." \
  -d '{
    "amount": 100,
    "fiat_currency": "CNY",
    "provider": "alchemy_pay"
  }'
```

---

## 其他支付提供者

### Unlimit

可以按照Alchemy Pay的模式添加Unlimit提供者：

1. 创建 `api/services/payment_providers/unlimit.py`
2. 实现 `process_payment` 方法
3. 在 `PaymentService` 中注册

---

## Mock模式

如果未配置支付提供者，系统会自动使用Mock模式：
- ✅ 所有功能正常工作
- ✅ 不会产生真实费用
- ⚠️ 仅用于开发和测试

---

## 安全注意事项

1. **API密钥安全**
   - 不要将API密钥提交到Git
   - 使用环境变量存储
   - 定期轮换密钥

2. **Webhook验证**
   - 始终验证Webhook签名
   - 使用HTTPS
   - 验证订单ID和金额

3. **错误处理**
   - 记录所有支付错误
   - 实现重试机制
   - 监控异常交易

