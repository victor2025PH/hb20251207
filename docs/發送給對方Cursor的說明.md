# 📤 發送給另一個 Cursor 的說明

---

## 📁 需要發送的文件

請將以下 **2 個文件** 發送給負責 AI 聊天控制後台的 Cursor：

| # | 文件 | 路徑 | 說明 |
|---|------|------|------|
| 1 | **對接指南** | `c:\hbgm001\docs\AI系統對接指南_發送給對方.md` | 完整的 API 文檔 |
| 2 | **Python SDK** | `c:\hbgm001\docs\lucky_red_ai_sdk.py` | 可直接使用的 SDK |

---

## 💬 發送給對方的訊息模板

複製以下內容發送給對方 Cursor：

```
@Cursor AI 聊天控制後台開發者

我是紅包遊戲後端開發者，現在需要與您對接。

請閱讀以下兩個文件：

1. 【對接指南】- 詳細說明所有 API 端點、認證方式、請求/響應格式
2. 【Python SDK】- 可以直接複製到您的項目中使用

=== 快速開始 ===

1. 安裝依賴：
   pip install httpx

2. 使用 SDK：
   from lucky_red_ai_sdk import LuckyRedAIClient
   
   client = LuckyRedAIClient(
       api_key="[我會提供給您]",
       base_url="http://localhost:8080"
   )
   
   # 查詢用戶餘額
   result = client.get_balance(telegram_user_id=123456789)
   
   # 發送紅包
   result = client.send_packet(
       telegram_user_id=123456789,
       total_amount=10.0,
       total_count=5,
       message="AI 紅包"
   )
   
   # 領取紅包
   result = client.claim_packet(
       telegram_user_id=987654321,
       packet_uuid=result.data['packet_id']
   )

=== API 端點摘要 ===

GET  /api/v2/ai/status              - 健康檢查
GET  /api/v2/ai/wallet/balance      - 查詢餘額
GET  /api/v2/ai/user/profile        - 用戶資料
POST /api/v2/ai/packets/send        - 發送紅包
POST /api/v2/ai/packets/claim       - 領取紅包
POST /api/v2/ai/wallet/transfer     - 內部轉帳
GET  /api/v2/ai/packets/{uuid}      - 紅包詳情

=== 認證方式 ===

每個請求需要以下 Headers：
- Authorization: Bearer <API_KEY>
- X-Telegram-User-Id: <代表的用戶 Telegram ID>

請告訴我：
1. 您需要什麼額外功能？
2. 需要測試環境 API Key 嗎？
3. 有任何技術問題嗎？
```

---

## 🔑 API Key 配置

對方需要 API Key 才能調用您的 API。您可以：

### 方式 1：測試環境（開發階段）

在您的 `.env` 文件中：
```env
DEBUG=true
AI_API_KEY=
```
> 留空 API Key，開發模式下允許任意 Key

### 方式 2：生產環境

在您的 `.env` 文件中設置正式 Key：
```env
DEBUG=false
AI_API_KEY=your-secure-api-key-here
```
然後將這個 Key 提供給對方。

---

## 📋 對接清單

### 您（紅包遊戲後端）需要做的：

- [x] 創建 AI API 端點 (`/api/v2/ai/*`)
- [x] 創建對接文檔
- [x] 創建 Python SDK
- [ ] 提供 API Key 給對方
- [ ] 啟動測試服務器
- [ ] 協助對方測試

### 對方（AI 聊天後台）需要做的：

- [ ] 閱讀對接文檔
- [ ] 集成 Python SDK
- [ ] 實現 AI 指令解析
- [ ] 調用紅包 API
- [ ] 處理響應並回覆用戶

---

## 🧪 測試建議

### 啟動本地測試服務器

```powershell
cd c:\hbgm001
python -m api.main
```

### 測試 API 是否正常

```powershell
curl http://localhost:8080/api/v2/ai/status
```

### 對方測試流程

1. 使用您提供的 API Key
2. 先調用 `/status` 確認連通
3. 調用 `/wallet/balance` 測試認證
4. 調用 `/packets/send` 測試發紅包
5. 調用 `/packets/claim` 測試領紅包

---

## 📞 後續溝通

如果對方有問題，常見解答：

| 問題 | 解答 |
|------|------|
| "401 Unauthorized" | 檢查 API Key 和 Headers |
| "404 User not found" | 用戶需要先在 Telegram Bot 中註冊 |
| "Insufficient balance" | 用戶餘額不足，需要充值 |
| "Already claimed" | 用戶已經領取過這個紅包 |

---

**祝對接順利！如有問題隨時溝通。**
